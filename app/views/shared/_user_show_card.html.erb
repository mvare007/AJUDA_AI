<div class="card" style='background: yellow;'>
  <div class="card-body text-center">
    <img class="avatar-xl rounded-circle my-2" src="https://eu.ui-avatars.com/api/?name=<%= user.full_name.gsub(" ", "+") %>}" alt="Bologna">
    <h2 class="card-title"><%= user.full_name %></h2>
    <h4 class="card-subtitle mb-5 text-muted"><%= user.city %></h4>
    <h5 class="card-text my-5"><%= user.about_me %></h5>
    <p class="br-wrapper br-theme-css-stars"><%= @mean %></p>
    <a href="#" class="btn btn-info mx-2"><span class="far fa-envelope"></span> Mensagem</a>
    <% if current_user.favorited?(user, scope: :follow) %>
      <%= link_to 'A seguir', user_follow_path(user), class: "btn btn-warning"  %>
    <% else %>
      <%= link_to 'Seguir', user_follow_path(user), class: "btn btn-info" %>
    <% end %>
    <hr style="border: 1px solid #341D48;">
    <div class="row">
      <div class="col-sm-12 col-md-8 col-lg-4">
        <%= render 'shared/following' %>
      </div>
      <div class="col-sm-12 col-md-8 col-lg-4">
        <%= render 'shared/followers' %>
      </div>
      <div class="col-sm-12 col-md-12 col-lg-4">
        <%= render 'shared/requests_from_user' %>
      </div>
    </div>
  </div>
</div>
