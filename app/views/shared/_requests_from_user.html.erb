<h1 id="userRequests"><%= @user.requests.count %></h1>
<span id="settingsUserRequests">Pedidos</span>
<div id="panelUserRequests">
  <ul>
    <% @user.requests.each do |request| %>
      <%= render 'shared/request_card', request: request %>
    <% end %>
  </ul>
</div>
